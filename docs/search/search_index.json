{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Eventify","text":"<p>Eventify is a Java event-sourcing library backed by Apache Kafka. You define commands, events and an aggregate; Eventify records events and rebuilds aggregate state when a command arrives.</p>"},{"location":"#the-model","title":"The model","text":"<ul> <li>A command asks to change one aggregate.</li> <li>A command handler validates that request and returns event payloads.</li> <li>An event is an immutable fact.</li> <li>An event-sourcing handler applies an event to produce the next aggregate state.</li> <li>An event handler reacts to published events, for example to update a view or send a notification.</li> </ul> <p>Start with Getting Started, then define your domain model and handlers.</p>"},{"location":"#important-choices","title":"Important choices","text":"<ul> <li>Keep aggregates, commands and events immutable.</li> <li>Use the aggregate id as the command key.</li> <li>Keep event-sourcing handlers deterministic and free of side effects.</li> <li>Enable snapshots only when replaying a long history becomes expensive. With <code>deleteEvents = true</code>, history before a snapshot is no longer available.</li> </ul>"},{"location":"#more","title":"More","text":"<ul> <li>Command Gateway: send commands from an API or application.</li> <li>Advanced features: snapshots and event upcasting.</li> <li>Testing: test a complete topology in memory.</li> <li>Eventify Console: inspect aggregate history in an ops UI.</li> </ul>"},{"location":"advanced/","title":"Advanced Features","text":""},{"location":"advanced/#snapshots","title":"Snapshots","text":"<p>Eventify rebuilds an aggregate from its events. Add a snapshot when a long history makes that too expensive.</p> <pre><code>@AggregateRoot(\"order\")\n@EnableSnapshotting(threshold = 500)\npublic class Order { ... }\n</code></pre> <p>Eventify saves a snapshot after a successful command when the threshold is crossed. Handlers need no special code.</p> Setting Meaning <code>threshold</code> Create a snapshot every N events. <code>deleteEvents</code> Remove events before the snapshot after it is safely stored. Defaults to <code>false</code>. <p>Keep <code>deleteEvents</code> disabled when you need full audit history or time travel before the snapshot. When it is enabled, Eventify fails safely if an old snapshot cannot be used.</p>"},{"location":"advanced/#aggregate-changes","title":"Aggregate changes","text":"<p>Raise the aggregate's <code>@Revision</code> when a changed field or <code>@EventSourcingHandler</code> method would produce a different state from the same events.</p> <pre><code>@AggregateRoot(\"order\")\n@Revision(2)\n@EnableSnapshotting(threshold = 500)\npublic class Order { ... }\n</code></pre> <p>Old snapshots are then ignored and rebuilt from events when possible.</p>"},{"location":"advanced/#event-upcasting","title":"Event upcasting","text":"<p>Upcasters transform old event JSON into the current event shape while reading history. Increase an event's <code>@Revision</code> and add one <code>@Upcaster</code> method per step.</p> <pre><code>@Revision(2)\npublic record OrderPlaced(@AggregateId String id, String customer, String channel) {}\n\npublic class OrderUpcaster {\n    @Upcaster(type = \"com.example.OrderPlaced\", revision = 1)\n    public JsonNode addChannel(ObjectNode json) {\n        json.put(\"channel\", \"web\");\n        return json;\n    }\n}\n</code></pre> <p>Register the upcaster like any other handler. Keep old upcasters: older events may still need every step in the chain.</p>"},{"location":"annotation-reference/","title":"Annotation Reference","text":"Annotation Use <code>@Topic(\"...\")</code> Topic for a command or event type. <code>@AggregateId</code> The aggregate identifier field on a command, event or aggregate. <code>@AggregateRoot(\"...\")</code> Stable name of an aggregate type. <code>@CommandHandler</code> Decides which events a command produces. <code>@EventSourcingHandler</code> Produces the next aggregate state from an event. <code>@EventHandler</code> Reacts to a published event outside the aggregate. <code>@EnableSnapshotting</code> Enables periodic snapshots. <code>@Revision(n)</code> Version of an event schema or aggregate snapshot shape. <code>@Upcaster(type, revision)</code> Migrates an older event shape while reading it. <code>@Priority(n)</code> Order for handlers of the same event; high runs first. <code>@Timestamp</code> Injects a message timestamp. <code>@MessageId</code> Injects a message id. <code>@MetadataValue(\"key\")</code> Injects one metadata value."},{"location":"command-gateway/","title":"Command Gateway","text":"<p>Use <code>CommandGateway</code> at your application boundary to send a command and receive its outcome.</p> <pre><code>CommandGateway gateway = CommandGateway.builder()\n    .producerConfig(producerConfig)\n    .replyTopic(\"orders.replies\")\n    .build();\n</code></pre>"},{"location":"command-gateway/#send-a-command","title":"Send a command","text":"<pre><code>CompletableFuture&lt;CommandResult.Success&gt; result = gateway.send(\n    new PlaceOrder(\"order-1\", \"Ada\")\n);\n\nCommandResult.Success completed = gateway.sendAndWait(\n    new PlaceOrder(\"order-1\", \"Ada\")\n);\n</code></pre> <p><code>send</code> is asynchronous. <code>sendAndWait</code> blocks until a result or timeout. A rejected command completes exceptionally with <code>CommandExecutionException</code>.</p>"},{"location":"command-gateway/#metadata","title":"Metadata","text":"<p>Wrap a payload in <code>Command</code> when you want to add metadata.</p> <pre><code>gateway.send(Command.builder()\n    .payload(new PlaceOrder(\"order-1\", \"Ada\"))\n    .metadata(Metadata.of(\"tenant\", \"acme\"))\n    .build());\n</code></pre> <p>Eventify passes command metadata to produced events and adds correlation and causation ids.</p> <p>A timeout means the caller did not receive a result in time; it does not prove that the command was not handled.</p>"},{"location":"console/","title":"Eventify Console","text":"<p>Eventify Console is an ops UI for inspecting aggregates, events and command outcomes.</p>"},{"location":"console/#run-it","title":"Run it","text":"<pre><code>docker run -p 8080:8080 ghcr.io/alikelleci/eventify-console:latest\n</code></pre> <p>Open <code>http://localhost:8080</code>.</p>"},{"location":"console/#connect-an-application","title":"Connect an application","text":"<p>Add the client dependency and register the plugin.</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.alikelleci&lt;/groupId&gt;\n    &lt;artifactId&gt;eventify-console-client&lt;/artifactId&gt;\n    &lt;version&gt;x.y.z&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <pre><code>Eventify eventify = Eventify.builder()\n    .streamsConfig(config)\n    .registerPlugin(EventifyConsoleClient.builder()\n        .url(\"http://localhost:8080\")\n        .build())\n    .build();\n</code></pre> <p>Connected applications appear automatically. The console can show stored events, aggregate state and command results.</p>"},{"location":"console/#secure-it","title":"Secure it","text":"<p>For production, protect both the UI and application connection:</p> <ul> <li>Set <code>EVENTIFY_CONSOLE_APPTOKEN</code> on the console and pass the same value with <code>.token(...)</code> in each client.</li> <li>Configure OpenID Connect with <code>EVENTIFY_CONSOLE_OIDC_ISSUERURI</code>, <code>EVENTIFY_CONSOLE_OIDC_CLIENTID</code> and <code>EVENTIFY_CONSOLE_OIDC_CLIENTSECRET</code>.</li> </ul> <p>The redirect URI is <code>https://&lt;console-address&gt;/login/oauth2/code/sso</code>.</p>"},{"location":"domain-modeling/","title":"Domain Modeling","text":"<p>An aggregate is one consistency boundary. Give it a stable name and an identifier; commands and events for that aggregate carry the same id.</p> <pre><code>@Value\n@Builder(toBuilder = true)\n@AggregateRoot(\"order\")\npublic class Order {\n    @AggregateId String id;\n    String customer;\n    String trackingNumber;\n}\n</code></pre> <p>Use an immutable class. The name in <code>@AggregateRoot</code> identifies stored history, so keep it stable.</p>"},{"location":"domain-modeling/#commands-and-events","title":"Commands and events","text":"<p>Commands express intent; events express facts. Both need exactly one <code>@AggregateId</code> field. Group related types under one <code>@Topic</code>.</p> <pre><code>@Topic(\"commands.order\")\npublic interface OrderCommand {\n    record Place(@AggregateId String id, String customer) implements OrderCommand {}\n    record Ship(@AggregateId String id, String trackingNumber) implements OrderCommand {}\n}\n\n@Topic(\"events.order\")\npublic interface OrderEvent {\n    record Placed(@AggregateId String id, String customer) implements OrderEvent {}\n    record Shipped(@AggregateId String id, String trackingNumber) implements OrderEvent {}\n}\n</code></pre> <p>Use Bean Validation annotations on command fields when input validation belongs with the command.</p>"},{"location":"domain-modeling/#rules-of-thumb","title":"Rules of thumb","text":"<ul> <li>Choose event names in past tense: <code>OrderPlaced</code>, not <code>PlaceOrder</code>.</li> <li>Put business time in the event payload; Eventify's timestamp is recording time.</li> <li>Never change an existing event's meaning. Add an upcaster when its shape changes.</li> <li>A removed aggregate is represented by an event whose <code>@EventSourcingHandler</code> method returns <code>null</code>.</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Add Eventify, define your domain, register handlers and start the application.</p>"},{"location":"getting-started/#what-you-need","title":"What you need","text":"<p>Access to an Apache Kafka cluster.</p>"},{"location":"getting-started/#add-the-dependency","title":"Add the dependency","text":"<pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.alikelleci&lt;/groupId&gt;\n    &lt;artifactId&gt;eventify-core&lt;/artifactId&gt;\n    &lt;version&gt;x.y.z&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>For Spring Boot, use <code>eventify-spring-boot-starter</code> instead.</p>"},{"location":"getting-started/#define-a-small-domain","title":"Define a small domain","text":"<pre><code>@Topic(\"commands.order\")\npublic record PlaceOrder(@AggregateId String id, String customer) {}\n\n@Topic(\"events.order\")\npublic record OrderPlaced(@AggregateId String id, String customer) {}\n\n@AggregateRoot(\"order\")\npublic record Order(@AggregateId String id, String customer) {}\n</code></pre>"},{"location":"getting-started/#add-the-behaviour","title":"Add the behaviour","text":"<pre><code>public class OrderHandler {\n    @CommandHandler\n    public OrderPlaced handle(PlaceOrder command, Order state) {\n        if (state != null) throw new ValidationException(\"Order already exists\");\n        return new OrderPlaced(command.id(), command.customer());\n    }\n\n    @EventSourcingHandler\n    public Order handle(OrderPlaced event, Order state) {\n        return new Order(event.id(), event.customer());\n    }\n}\n</code></pre> <p>The command handler decides which event to record. The <code>@EventSourcingHandler</code> method is the only place that changes aggregate state.</p>"},{"location":"getting-started/#start-eventify","title":"Start Eventify","text":"<pre><code>Properties config = new Properties();\nconfig.put(StreamsConfig.APPLICATION_ID_CONFIG, \"orders\");\nconfig.put(StreamsConfig.BOOTSTRAP_SERVERS_CONFIG, \"localhost:9092\");\n\nEventify eventify = Eventify.builder()\n    .streamsConfig(config)\n    .registerHandler(new OrderHandler())\n    .build();\n\neventify.start();\n</code></pre> <p>Use the aggregate id as the key when sending a command. Command Gateway is the simplest way to do that.</p>"},{"location":"getting-started/#spring-boot","title":"Spring Boot","text":"<p>Add the starter, expose an <code>Eventify</code> bean, and make handler classes Spring beans. The starter registers those handlers and starts Eventify.</p> <pre><code>@Bean\nEventify eventify(Eventify.EventifyBuilder builder) {\n    return builder.streamsConfig(config).build();\n}\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li>Domain modeling</li> <li>Handlers</li> <li>Testing</li> </ul>"},{"location":"handlers/","title":"Handlers","text":"<p>Handlers are plain Java methods. Register the object that contains them with <code>Eventify</code>.</p>"},{"location":"handlers/#handle-a-command","title":"Handle a command","text":"<p>A command handler receives a command and the current aggregate state. Return one event, a list of events, or no event.</p> <pre><code>@CommandHandler\npublic OrderEvent handle(ShipOrder command, Order state) {\n    if (state == null) throw new ValidationException(\"Order does not exist\");\n    if (state.trackingNumber() != null) return null;\n    return new OrderShipped(command.id(), command.trackingNumber());\n}\n</code></pre> <p>Throw an exception to reject the command. A successful command with no events is allowed.</p>"},{"location":"handlers/#apply-an-event","title":"Apply an event","text":"<p>An event-sourcing handler returns the next aggregate state.</p> <pre><code>@EventSourcingHandler\npublic Order handle(OrderShipped event, Order state) {\n    return state.toBuilder().trackingNumber(event.trackingNumber()).build();\n}\n</code></pre> <p>Keep <code>@EventSourcingHandler</code> methods deterministic and side-effect free. They run whenever Eventify rebuilds state. Return <code>null</code> to remove an aggregate.</p> <p>An event without an <code>@EventSourcingHandler</code> method still becomes part of the history and advances the aggregate version; it simply leaves state unchanged.</p>"},{"location":"handlers/#react-to-an-event","title":"React to an event","text":"<p>Use <code>@EventHandler</code> for work outside the aggregate, such as projections or notifications.</p> <pre><code>@EventHandler\npublic void handle(OrderPlaced event) {\n    ordersView.insert(event.id(), event.customer());\n}\n</code></pre> <p>Make side effects idempotent. A handler may run again after recovery.</p>"},{"location":"handlers/#available-message-values","title":"Available message values","text":"<p>Besides the payload and aggregate state, handlers can receive:</p> Parameter Value <code>@AggregateRoot</code> type Current state; required for <code>@CommandHandler</code> <code>Metadata</code> All message metadata <code>@Timestamp Instant</code> Message timestamp <code>@MessageId String</code> Message id <code>@MetadataValue(\"key\") String</code> One metadata value"},{"location":"handlers/#ordering","title":"Ordering","text":"<p>Use <code>@Priority</code> only when multiple <code>@EventHandler</code> methods handle the same event. Higher values run first.</p>"},{"location":"handlers/#spring-listeners","title":"Spring listeners","text":"<p>The Spring Boot starter can also deserialize Eventify events for <code>@KafkaListener</code> methods. Use that when a projection should have its own lifecycle and error handling; command handling always uses <code>@CommandHandler</code>.</p>"},{"location":"testing/","title":"Testing","text":"<p>Use Kafka Streams' <code>TopologyTestDriver</code> to test commands, events and results without a broker.</p> <pre><code>Eventify eventify = Eventify.builder()\n    .streamsConfig(testConfig)\n    .registerHandler(new OrderHandler())\n    .build();\n\ntry (TopologyTestDriver driver = new TopologyTestDriver(eventify.topology())) {\n    var commands = driver.createInputTopic(\n        \"commands.order\", new StringSerializer(), new CommandSerde().serializer());\n    var results = driver.createOutputTopic(\n        \"commands.order.results\", new StringDeserializer(), new JsonDeserializer&lt;&gt;(CommandResult.class));\n\n    Command command = Command.builder().payload(new PlaceOrder(\"order-1\", \"Ada\")).build();\n    commands.pipeInput(command.getAggregateId(), command);\n\n    assertThat(results.readValue()).isInstanceOf(CommandResult.Success.class);\n}\n</code></pre> <p>Test both successful decisions and rejected business rules. When helpful, inspect the emitted event topic or the <code>event-store</code> and <code>snapshot-store</code> test stores directly.</p>"}]}