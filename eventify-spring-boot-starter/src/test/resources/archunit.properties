# Rules frozen with FreezingArchRule keep their known violations here, in the repository.
# A new violation fails the build; a fixed one is removed from the store automatically.
freeze.store.default.path=src/test/resources/archunit_store
freeze.store.default.allowStoreCreation=true
